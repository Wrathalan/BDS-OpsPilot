# Generated endpoint downloads

`npm run agent:build:windows` places the self-contained Windows x64 agent and its SHA-256 file here. Generated executables and checksums are intentionally ignored by Git. The Docker build produces the same executable in this directory before the Next.js production build.
